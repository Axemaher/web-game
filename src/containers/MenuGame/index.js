import React from 'react'
import styled from 'styled-components'

const StyledNav = styled.nav`
    height: inherit;
    width: 200px;
    background-color: white;
`;

const MenuGame = () => {
    return (
        <StyledNav>menu game</StyledNav>
    );
}

export default MenuGame;